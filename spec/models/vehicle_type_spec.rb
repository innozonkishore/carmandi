require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe VehicleType do
  
  # def valid_vehicle_type_attributes
  #   { :name => 'any vehicle type' }
  # end
  # 
  # it "should invalidate without a name" do
  #   VehicleType.should need(:name).using(valid_vehicle_type_attributes)
  # end
  # 
  # it "should need to have its name unique" do
  #   VehicleType.should need(:name).to_be_unique.using(valid_vehicle_type_attributes)
  # end


end
