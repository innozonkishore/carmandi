<%if @links.blank? %>
<div class = "admin_links">
  <p>
    <%= t(:no_link_yet) %> 
  </p>
  <p>
    <%= t(:try_creating_one_now) %> <%=link_to t(:clicking_here), new_admin_link_path %>
  </p>
</div>
<% else %>
<div class="active-scaffold active-scaffold-as_admin__representative admin/representative-view default-theme" id="as_admin__representative-active-scaffold">
  <div class="active-scaffold-header">
    <div class="actions">
      <%=link_to t(:create_new), new_admin_link_path, :class => 'new action' %>
    </div>
    <h2>
      <%= t(:Links) %>
    </h2>
  </div>
  <div id="as_admin__representative-content">
    <table cellspacing="0" cellpadding="0">
      <thead>
        <tr>
          <th title="">
            <a>
              <%= t(:Name) %>
            </a>
          </th>
          <th title="">
            <a>
              <%= t(:Position) %>
            </a>
          </th>
          <th title="">
            <a>
              <%= t(:Link_location) %>
            </a>
          </th>
          <th title="">
            <a>
              <%= t(:Status) %>
            </a>
          </th>
          <th class="actions">
          </th>
        </tr>
      </thead>
      <tbody id="as_admin__representative-tbody" class="records">
        <% for link in @links %>
        <tr id="as_admin__representative-list-3-row" class="record">
          <td class="name-column sorted">
            <%= link.name_english %>
          </td>
          <td>
            <%= link.position %>
					</td>
					<td>
            <% if link.useful == false %>
							<%= t(:important) %>
						<% else %>
							<%= t(:useful) %>
						<% end %>
          </td>
          <td>
            <%= link.status %>
          </td>
          <td class="actions">
            <table cellspacing="0" cellpadding="0">
              <tbody>
                <tr>
                  <td class="indicator-container">
                    <img style="visibility: hidden;" src="/images/active_scaffold/default/indicator.gif?1250079352" id="as_admin__representative-record-3-loading-indicator" class="loading-indicator" alt="loading indicator"/>
                  </td>
                  <td>
                    <%= link_to t(:Edit), edit_admin_link_path(link) %>
                  </td>
                </tr>
              </tbody>
            </table>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<% end %>